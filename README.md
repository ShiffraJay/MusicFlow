# MusicFlow
MusicFlow is an AI-based hybrid music recommendation system using SVD, KMeans, and cosine similarity. It provides personalized suggestions via a Flask backend and user feedback loop. A trained model enables fast, accurate recommendation generation.
